@page "{id:int}"
@model EVCharging.Pages.Admin.AdminServicePlanPage.DeleteModel
@{
    Layout = "_AdminLayout";
}


<div class="container mt-4">
    <h3 class="fw-bold mb-4 text-warning">
        <i class="fa-solid fa-triangle-exclamation me-2"></i>
        Xóa gói dịch vụ
    </h3>

    <div class="card p-4 bg-dark border border-warning shadow-lg">
        <div class="mb-3">
            <h5 class="text-light">
                Bạn có chắc chắn muốn xóa gói dịch vụ
                <span class="text-warning fw-bold">"@Model.Plan.Name"</span> không?
            </h5>
            <p class="text-secondary">
                Sau khi xóa, gói dịch vụ sẽ không hiển thị trong danh sách, nhưng vẫn được lưu trong hệ thống để đảm bảo an toàn dữ liệu.
            </p>
        </div>

        <dl class="row small text-light mb-3">
            <dt class="col-sm-3">Mã gói:</dt>
            <dd class="col-sm-9">@Model.Plan.Id</dd>

            <dt class="col-sm-3">Giá:</dt>
            <dd class="col-sm-9">@string.Format("{0:N0} VNĐ", Model.Plan.Price)</dd>

            <dt class="col-sm-3">Mô tả:</dt>
            <dd class="col-sm-9">@Model.Plan.Description</dd>
        </dl>

        <form method="post">
            <input type="hidden" asp-for="Plan.Id" />
            <div class="text-end mt-4">
                <a asp-page="Index" class="btn btn-outline-light me-2">
                    <i class="fa-solid fa-arrow-left"></i> Quay lại
                </a>
                <button type="submit" class="btn btn-warning text-dark fw-bold">
                    <i class="fa-solid fa-trash"></i> Xác nhận xóa
                </button>
            </div>
        </form>
    </div>
</div>